import 'package:flutter/material.dart';

class RecipeDetailPage extends StatelessWidget {
  static const String $PATH = 'RecipeDetail';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("RecipeDetail"),
    );
  }
}
